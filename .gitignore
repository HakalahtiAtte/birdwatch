# Environment files - never commit these
.env
.env.local
.env*.local
mobile/.env
web/.env.local

# Dependencies
node_modules/
mobile/node_modules/
web/node_modules/

# Expo
mobile/.expo/
mobile/dist/

# Next.js
web/.next/
web/out/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Data files — local only
*.csv
*.xlsx

# Firebase — contains project config, not secrets, but no need to commit
mobile/google-services.json

# Scripts dependencies (root-level, one-time use)
package.json
package-lock.json

# Supabase CLI temp files
supabase/.temp/

# Expo local config
.expo/

# Claude Code local settings (machine-specific, not for sharing)
.claude/settings.local.json